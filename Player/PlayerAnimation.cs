namespace ActionRPG.Player
{
    /// <summary>
    /// The different animations available on the player scene.
    /// </summary>
    public enum PlayerAnimation
    {
        Idle,
        Run,
        Roll,
        Attack
    }
}